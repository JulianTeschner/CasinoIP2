name: frontend pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # test:
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run: 
  #       working-directory: ./frontend/casino-ip2

  #   steps:
  #   - name: checkout
  #     uses: actions/checkout@v3
  #   - uses: actions/cache@v3
  #     with:
  #       path: ~/.npm
  #       key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
  #       restore-keys: |
  #         ${{ runner.os }}-node-

  #   - name: test coverage
  #     uses: artiomtr/jest-coverage-report-action@v2
  #     with:
  #       working-directory: ./frontend/casino-ip2
  #       github-token: ${{ secrets.GITHUB_TOKEN }}
  #       test-script: npm run test -- --watchAll=false --coverage --testLocationInResults --json --outputFile="report.json"
  #       threshold: 50

  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run: 
        working-directory: ./frontend/casino-ip2

    steps:
    - name: checkout
      uses: actions/checkout@v3

    - uses: actions/cache@v3
      with:
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-

    - name: Setup Node.js environment
      uses: actions/setup-node@v3
      with: 
        node-version: 16
    
    - name: install node dependencies
      run: npm install
    
    - name: run tests
      run: npm run test

    - name: coverage
      uses: codecov/codecov-action@v2
      with:
        directory: ./frontend/casino-ip2
        token: ${{ secrets.CODECOV_TOKEN }}
        file: ./coverage/clover.xml
        flags: unittests # optional
        name: codecov-umbrella # optional

    - name: build app
      run: npm run build

  
  deploy:
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - uses: actions/checkout@v3
      - uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: ${{secrets.HEROKU_KEY}}  
          heroku_app_name: "casino-frontend1"
          heroku_email: ${{secrets.HEROKU_MAIL}} 
          buildpack: "https://github.com/mars/create-react-app-buildpack"
          appdir: "./frontend/casino-ip2"
          usedocker: false
